# Copy this file to deploy/oracle/.env on the Oracle VM. Never commit .env.
POSTGRES_PASSWORD=replace-with-a-new-random-password
COMIC_FICP_KEY_ENCRYPTION_SECRET=replace-with-a-new-random-encryption-secret
COMIC_FICP_PUBLIC_PORT=8501
